# Run outputs

Run artifacts are written here by default and ignored by Git. Each run directory must contain
the resolved configuration, environment and Git provenance, logs, checkpoints, predictions,
metrics, and links or hashes for its input manifests and auxiliary caches.
