# Protocol manifests

NTU protocol files are generated deterministically from official sample names. Toyota CS/CV1/CV2
requires the locally licensed official split lists. Protocol files contain complete `train` and
`test` sample-ID arrays and are validated against the manifest before any run. Do not publish
subject-identifying split metadata unless the dataset terms permit it.
