# Metadata

This directory contains redistributable subset definitions and schemas for local dataset
metadata. Restricted videos, annotations, subject identities, and private paths are intentionally
not part of the repository. Generate manifests under `metadata/manifests/` from locally licensed
data and keep them private unless the applicable dataset terms permit redistribution.
