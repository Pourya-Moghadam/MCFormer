# Diagnostic subsets

`toyota_diagnostics.json` contains the fixed manuscript display-name subsets. The analysis command
resolves those names against the run's canonical Toyota label map and fails if any name is missing
or ambiguous.
